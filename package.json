{
  "name": "ifelse-loader",
  "version": "1.0.0",
  "description": "ifelse-loader for webpack&node to support common code between frontend and backend",
  "main": "ifelse-loader.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "webpack",
    "loader",
    "if-loader",
    "ifelse-loader"
  ],
  "author": "folgerfan",
  "license": "MIT",
  "dependencies": {
    "loader-utils": "^1.1.0",
    "make-dir": "^1.0.0"
  }
}
